const INITIAL_STATE = { requests: 0, error: null, draughts: {}}

const placeDraughtsReducer = (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default placeDraughtsReducer;

